# 举报处理系统 需求文档

## 1. 背景与现状
- 现状：没有单独的用户举报功能，仅有一个置顶帖子，用户反馈后由管理员手动打开页面查看内容判断后手动处理（删帖或封禁用户）
- 痛点：管理员需要 手动打开页面——找到被举报的帖子——手动处理；

## 2. 目标
- 优化管理员处理举报的效率。通过新增“举报清单”页面，让被举报的帖子快速地显示在页面，并有处理按钮（删帖、封禁用户），帮助管理员快速判断和处理。
- 限制条件：
  - 用户举报不能“过于方便”，避免轻而易举地胡乱举报增加管理员的工作量。故一定程度保留填写楼层号等略微麻烦的操作、新增举报理由不少于5字的要求。


## 3. 基本使用流程
1. 举报流程
   1. 在回复组件(`\resources\vue\Thread\PostItem\PostItem.vue`)新增举报按钮，点击后跳转至“举报清单页面”（路由为 `/accuse?thread_id=xxx&floor=xxx`，由 PostItem 拼接 query 参数）；
   2. 跳转到”举报清单页面”后，自动弹出举报内容窗口，自动填写相应 thread_id 和 floor
   3. 上述floor也可以多个（一次举报多个回复），由用户自行填写，多个floor用逗号分隔。最多10个。一个举报信息对应一个举报单（可包含多个 floor），管理员按举报单整体处理。
      提交时前端需校验：无效 floor、重复 floor、超过10个。后端需要校验：主题不存在、楼层不存在
   4. 举报人需填写举报理由，不少于5字。
   5. 举报人提交后，在“举报清单页面”新增一条“举报信息”，显示为待处理状态。
   6. 提交或取消后清理 query，避免刷新重复弹窗。
   7. 任何用户都可以在“举报信息”中追加留言，进行追加说明或者申诉。
2. 管理员处理流程
   1. 管理员查看“举报清单页面”时，可以筛选未处理的“举报信息”进行处理
   2. 管理员确认内容后，可以选择4个处理方式。处理后，该”举报信息”被标记为”已处理状态”。
      - 忽略：直接将举报信息转为”已处理”状态，不执行任何操作。
      - 删帖：调用 `deletePostPoster`。
      - 锁定饼干：调用 `userLockPoster`。
      - 封禁饼干：调用 `userBanPoster`。
      - 上面3个调用均对应`resources/api/methods/admin.ts`的接口。
      - 删帖、锁定饼干、封禁饼干这3个处理方式都需要二次确认。
   3. 管理员处理的同时，可以选择标注“？”选项，说明这个处理不太确定，以引起其他管理员注意。这个“？”标记仅管理员可见，且仅管理员可关闭。这是一个与已处理/未处理无关的额外状态。
   4. 仅管理员可见该举报信息的举报人近7天的举报次数、和被举报人的近7天被举报次数。统计口径按“举报单次数”计数（一次提交记1次，即使包含多个 floor）。
3. 处理后反馈
   1. 目前项目内无站内信等系统，用户只能自行浏览“举报清单页面”确认被举报情况。
4. 匿名约束
   1. 举报人和被举报人身份信息对所有角色均不可见（包含管理员）。

## 4. 页面设计

### 4.1 举报清单页面
- 路由： `/accuse?thread_id=xxx&floor=xxx`
  - 若带有 `thread_id` 和 `floor` query 参数，页面加载后自动弹出新增举报 modal，并自动填写对应字段。
  - 若不带参数，则正常展示举报清单。
- 入口位置：
    1. 在`\resources\vue\TopBar\TopBar.vue` 的“个人中心”下方新增“举报中心”按钮；
    2. 在·的打赏按钮左侧新增一个举报按钮（图形化的），并且用更浅的次要颜色显示。这个按钮就是上述3.1.1提及的按钮。
- 页面布局： 
  - “举报清单页面”：参考 `\resources\vue\Thread\ThreadPage.vue`的结构
    - 每个“举报信息”是一个单文件组件、竖向排列。
    - 顶上左侧：“仅显示未处理”的switch，
    - 顶上右侧：新增举报按钮，
    - 底部右侧：是翻页导航条。
  - “举报信息”组件的显示内容：(整体外观风格参考 `\resources\vue\Thread\PostItem\PostItem.vue`)
    - 第一行左侧：处理状态（已处理、未处理），已处理的字体用绿色，未处理的字体用默认色；创建时间（所有人可见）
    - 第一行右侧：管理员处理按钮4个（忽略、删帖、锁定饼干、封禁饼干），1个“？”标记的可选框（管理员可关闭）；处理人（仅管理员可见）。这些内容仅管理员可见
    - 第二行：被举报回复的对应主题title。
    - 第三行：被举报回复的floor，可能有多个，横向排开。形如 №101 №104  №105。每个floor同时是个超链接，用新窗口跳转至相应主题页面。
    - 第四行：追加留言内容（所有人可见）；举报人近7天的举报次数、和被举报人的近7天被举报次数（仅管理员可见）。
    - 第五行：处理备注（仅管理员可见）。
    - 第六行：追加留言的填写框和按钮，单行div。当举报已处理时，不显示此行。
  - 新增举报的弹出modal （风格参考 `\resources\vue\Components\PostInput\HongbaoModal.vue`）
    - 触发条件：路由带有 `thread_id` query 参数时自动弹出；也可通过页面顶部"新增举报"按钮手动打开。
    - 第一行：举报主题ID（`n-input-number`）。若从 PostItem 跳转过来，已通过 query 参数自动填写。
    - 第二行：举报回复floor（`n-input`，单行文本框）。若从 PostItem 跳转过来，已自动填写第一个 floor。用户可追加其他 floor，逗号分隔，最多10个；floor 仅允许正整数。
    - 第三行：举报理由（`n-input` type=textarea，多行文本框），不少于5字。
    - modal底部：取消按钮、提交按钮。
- 排序规则：按数据库主键id倒序（由于数据库计划使用自增id，相当于是按日期倒序）
- 分页：每10条一页
