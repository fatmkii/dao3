# 举报处理系统 需求文档

## 1. 背景与现状
- 现状：没有单独的用户举报功能，仅有一个置顶帖子，用户反馈后由管理员手动打开页面查看内容判断后手动处理（删帖或封禁用户）
- 痛点：管理员需要 手动打开页面——找到被举报的帖子——手动处理；

## 2. 目标
- 优化管理员处理举报的效率。通过新增“举报清单”页面，让被举报的帖子快速地显示在页面，并有处理按钮（删帖、封禁用户），帮助管理员快速判断和处理。
- 限制条件：
  - 用户举报不能“过于方便”，避免轻而易举地胡乱举报增加管理员的工作量。故一定程度保留填写楼层号等略微麻烦的操作、新增举报理由不少于5字的要求。


## 3. 基本使用流程
1. 举报流程
   1. 在回复组件(`\resources\vue\Thread\PostItem\PostItem.vue`)新增举报按钮，点击后跳转至“举报清单页面”（路由为 `/accuse?thread_id=xxx&post_id=xxx&floor=xxx`，由 PostItem 拼接 query 参数）；
   2. 跳转到”举报清单页面”后，自动弹出举报内容窗口，自动填写相应 thread_id、post_id 和 floor。
   3. 一个举报单只对应一个回复。由于项目存在 floor 号可能重复的固有问题，举报记录必须以 `post_id` 作为被举报回复的唯一标识，floor 仅作为展示和跳转辅助信息。
   4. 举报人需填写举报理由，不少于5字。
   5. 同一用户对同一个 post 只能举报 1 次。若不同用户举报同一个 post，则合并到同一个举报信息中，保留每次举报理由用于管理员查看，举报信息仍按同一个待处理项展示。
   6. 举报人提交后，在“举报清单页面”新增或更新一条“举报信息”，显示为待处理状态。
      提交时前端需校验：post_id、thread_id 是否存在且为有效正整数，floor 是否存在且为有效非负整数，举报理由不少于5字。后端需要校验：主题不存在、回复不存在、post_id 不属于该 thread_id、同一用户重复举报同一 post。
   7. 提交或取消后清理 query，避免刷新重复弹窗。
2. 管理员处理流程
   1. 管理员查看“举报清单页面”时，可以筛选未处理的“举报信息”进行处理
   2. 管理员确认内容后，可以通过下拉菜单选择6个处理方式。除“提示”外，处理后该”举报信息”被标记为”已处理状态”。
      - 提示：调用 `userCheckPoster` 查询该饼干状态，仅展示提示信息，不改变举报处理状态。
      - 忽略：直接将举报信息转为”已处理”状态，不执行任何操作。
      - 删帖：调用 `deletePostPoster`。
      - 删全：调用 `deleteAllPostPoster`，一口气删除该楼层用户在该主题的所有回复。
      - 锁定饼干：调用 `userLockPoster`。
      - 封禁饼干：调用 `userBanPoster`。
      - 上面操作均对应`resources/api/methods/admin.ts`的接口。
      - 忽略只需要二次确认，不需要填写原因。
      - 删帖、删全、锁定饼干、封禁饼干这4个处理方式都需要弹出确认框，管理员必须填写处理原因，处理原因需要写入数据库。
      - 删帖和删全时确认框需要有“罚款olo”的可选项，对应 `deletePostPoster` / `deleteAllPostPoster` 的 `reduce_olo` 参数。
   3. 管理员处理的同时，可以选择标注“？”选项，说明这个处理不太确定，以引起其他管理员注意。这个“？”标记仅管理员可见，且仅管理员可关闭。这是一个与已处理/未处理无关的额外状态。
   4. 仅管理员可见举报人近7天的举报次数、和被举报回复作者近7天被举报次数。统计口径按“举报提交次数”计数；不同用户举报同一个 post 时，各自的提交均计入举报人统计，但举报清单仍合并为同一个举报信息。
      - 举报人近7天举报次数跟随每条举报理由显示，放在该条举报理由的举报日期后面。
      - 被举报回复作者近7天被举报次数跟随被举报楼层显示，放在楼层号后面。
3. 处理后反馈
   1. 目前项目内无站内信等系统，用户只能自行浏览“举报清单页面”确认被举报情况。
4. 匿名约束
   1. 举报人和被举报人身份信息对所有角色均不可见（包含管理员）。

## 4. 页面设计

### 4.1 举报清单页面
- 路由： `/accuse?thread_id=xxx&post_id=xxx&floor=xxx`
  - 若带有 `thread_id`、`post_id` 和 `floor` query 参数，页面加载后自动弹出新增举报 modal，并自动填写对应字段。
  - 若不带参数，则正常展示举报清单。
- 入口位置：
    1. 在`\resources\vue\TopBar\TopBar.vue` 的“个人中心”下方新增“举报中心”按钮；
    2. 在·的打赏按钮左侧新增一个举报按钮（图形化的），并且用更浅的次要颜色显示。这个按钮就是上述3.1.1提及的按钮。
- 页面布局： 
  - “举报清单页面”：参考 `\resources\vue\Thread\ThreadPage.vue`的结构
    - 每个“举报信息”是一个单文件组件、竖向排列。
    - 顶上左侧：“仅显示未处理”的switch，
    - 底部右侧：是翻页导航条。
  - “举报信息”组件的显示内容：(整体外观风格参考 `\resources\vue\Thread\PostItem\PostItem.vue`)
    - 第一行左侧：处理状态（已处理、未处理），已处理的字体用绿色，未处理的字体用默认色；创建时间（所有人可见）
    - 第一行右侧：管理员处理下拉菜单（提示、忽略、删帖、删全、锁定饼干、封禁饼干），1个“？”标记的可选框（管理员可关闭）。这些内容仅管理员可见。
    - 第二行：被举报回复的对应主题title，前面显示“主题：”。
    - 第三行：被举报回复的floor，前面显示“楼层：”。形如 楼层：№101。floor 是超链接，用新窗口跳转至相应主题页面；跳转 page 按每页 200 floor 计算，且 floor 从 0 开始，所以 page = `Math.floor(floor / 200) + 1`，hash 为 `#f_${floor}`。被举报回复作者近7天被举报次数放在楼层号后面，仅管理员可见。
    - 第四行：举报理由（所有人可见）。如果多个用户举报同一个 post，则展示多条举报理由；每条举报理由的举报日期后面显示该举报人近7天举报次数，仅管理员可见。
    - 第五行：处理人和处理原因（仅管理员可见），另起一行显示，不与举报次数放在同一行。删帖时如果勾选“罚款olo”，也在此处展示。
  - 新增举报的弹出modal （风格参考 `\resources\vue\Components\PostInput\HongbaoModal.vue`）
    - 触发条件：路由带有 `thread_id`、`post_id` 和 `floor` query 参数时自动弹出。用户不能在举报清单页面手动打开该 modal，因为 post_id 平时不可见，手动填写容易填错。
    - 隐含字段：举报回复ID `post_id`。从 PostItem 跳转过来后随表单状态保存并在提交时带上，但不展示给用户。
    - 第一行：举报主题ID。若从 PostItem 跳转过来，已通过 query 参数自动填写；该字段只展示，不允许编辑，避免与 post_id 不一致。
    - 第二行：举报回复floor。若从 PostItem 跳转过来，已通过 query 参数自动填写；该字段只展示，不允许编辑。floor 仅作为展示和跳转辅助信息，不作为唯一标识。
    - 第三行：举报理由（`n-input` type=textarea，多行文本框），不少于5字。
    - modal底部：取消按钮、提交按钮。
- 排序规则：按数据库主键id倒序（由于数据库计划使用自增id，相当于是按日期倒序）
- 分页：每10条一页

### 4.2 前端 demo 组件边界
- `AccusePage.vue`：举报清单页面容器，负责筛选、分页、query 参数触发弹窗、mock 数据加载。
- `AccuseItem.vue`：单条举报信息展示，负责处理状态、楼层链接、管理员操作按钮、“？”标记和处理备注展示。
- `AccuseCreateModal.vue`：新增举报表单，负责展示不可编辑的 thread_id 和 floor、隐含保存 post_id、填写举报理由和前端校验。
- `AccuseHandleModal.vue`：管理员处理弹窗，负责删帖、删全、锁定饼干、封禁饼干时填写处理原因；删帖和删全时额外提供“罚款olo”选项。
- `resources/api/methods/accuse.ts`：先定义 demo/mock 用的数据类型和方法，后续对接真实后端时替换为实际 API。
